export interface Book {
  title: string;
  author: string;
  description: string;
  ageRange?: string;
  coverImage: string;
  aiHint?: string;
}

export interface School {
  id: string;
  name:string;
}
